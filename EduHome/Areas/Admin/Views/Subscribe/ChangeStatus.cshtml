@using EduHome.Areas.Admin.ViewModels.SubscribeViewModels;

@{
    ViewData["Title"] = "ChangeStatus";
}

@model StatusSubscribeViewModel

<div class="col-md-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Change Status</h4>



            <h1>Is Active: @(Model.IsSubscribed ? "Yes" : "No")  </h1>

            @if (Model.IsSubscribed)
            {
                <form method="post">
                    <button type="submit" asp-action="ChangeStatus" class="btn btn-danger mr-2">Unsubscribe</button>
                </form>
            }
            else
            {
                <form method="post">

                    <button type="submit" asp-action="ChangeStatus" class="btn btn-primary mr-2">Subscribe</button>
                </form>
            }

        </div>
    </div>
</div>
