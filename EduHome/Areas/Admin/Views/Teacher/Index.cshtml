@using EduHome.Areas.Admin.ViewModels.TeacherViewModels;

@{
    ViewData["Title"] = "Teacher";
}

@model IEnumerable<TeacherViewModel>

<div class="content-wrapper">
    <div class="row">
        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Teachers</h4>

                    <div class="table-responsive">

                        <a class="btn btn-primary" asp-action="create">Create</a>
                        <table class="table">
                            <thead>
                                <tr>
                                    <th>IsDeleted</th>
                                    <th>Image</th>
                                    <th>Name</th>
                                    <th>Role</th>
                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (TeacherViewModel teacher in Model)
                                {
                                    <tr>

                                        <td>@teacher.IsDeleted</td>

                                        <td><img src="~/assets/img/teacher//@teacher.ImageName" /></td>
                                        <td>@teacher.Name</td>
                                        <td>@teacher.Role</td>

                                        <td><a class="btn btn-primary" asp-action="Details" asp-route-id="@teacher.Id">Detail</a></td>
                                        <td><a class="btn btn-warning" asp-action="Update" asp-route-id="@teacher.Id">Update</a></td>

                                        <td><a class="btn btn-danger" asp-action="Delete" asp-route-id="@teacher.Id">Delete</a></td>



                                    </tr>
                                }


                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>

    </div>
</div>
<!-- content-wrapper ends -->
